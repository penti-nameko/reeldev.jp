apiVersion: apps/v1
kind: Deployment
metadata:
  name: reeldevjp-bff
  labels:
    app: reeldevjp-bff
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reeldevjp-bff
  template:
    metadata:
      labels:
        app: reeldevjp-bff
    spec:
      containers:
        - name: bff
          image: ghcr.io/penti-nameko/reeldevjp-bff:latest
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: "3001"
            - name: NODE_ENV
              value: "production"
            - name: QIITA_USER
              valueFrom:
                configMapKeyRef:
                  name: reeldevjp-config
                  key: QIITA_USER
            - name: QIITA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: reeldevjp-secrets
                  key: QIITA_TOKEN
            - name: CORS_ORIGIN
              valueFrom:
                configMapKeyRef:
                  name: reeldevjp-config
                  key: CORS_ORIGIN
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
